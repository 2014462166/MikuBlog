# 🎵 音乐放这里（可选）

把 mp3 文件放进本目录后，在 `data/music.ts` 里把对应歌曲的 `src` 写成：

```ts
src: "/music/你下载的歌曲.mp3",
```

> GitHub 单文件建议 < 100MB（歌曲通常 3~10MB，没问题）。
> 也可以不用本目录，改用**阿里云 OSS 公共读直链**（详见 `data/music.ts` 顶部注释）。
